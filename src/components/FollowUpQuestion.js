const FollowUpQuestion = ({setQuestionState, question, answer, availablePoints, setMessage, nextQuestion, setCurrentPoints}) => {
    return ('Hi i am the follow up')    
}

export default FollowUpQuestion